{
  "name": "tauri-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "t-start": "npm run tauri dev",
    "t-build": "npm run tauri build",
    "t-window": "npm run tauri build --bundles msi,nsis",
    "android-init": "npm run tauri android init",
    "android-dev": "npm run tauri android dev",
    "andorid-build": "npm run tauri android build"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.1.1",
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-opener": "^2.4.0",
    "jquery": "^3.7.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.9.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.0.4"
  }
}
